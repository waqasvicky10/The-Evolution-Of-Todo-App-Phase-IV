apiVersion: v1
kind: Secret
metadata:
  name: todo-app-secret
  namespace: todo-app
type: Opaque
stringData:
  # Fixed secret key for development to persist sessions across restarts
  SECRET_KEY: "super-secret-fixed-key-for-phase-iv"
