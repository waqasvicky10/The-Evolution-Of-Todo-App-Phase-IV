# Kubernetes Secret Template - Phase IV
# Run: kubectl apply -f k8s/secrets-template.yaml
# Or manual: kubectl create secret generic todo-secrets --from-literal=database-url="sqlite:///./data/todo.db" --from-literal=auth-secret="your-secret-here" --from-literal=openai-key="sk-..."

apiVersion: v1
kind: Secret
metadata:
  name: todo-secrets
type: Opaque
stringData:
  database-url: "sqlite:///./data/todo.db"
  auth-secret: "change-me-hackathon-phase4"
  openai-key: "dummy-key-expires-soon"
