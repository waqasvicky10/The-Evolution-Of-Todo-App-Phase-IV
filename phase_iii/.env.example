# Phase III - Environment Variables Template
# 
# INSTRUCTIONS:
# 1. Copy this file: cp phase_iii/.env.example phase_iii/.env
# 2. Edit phase_iii/.env and add your actual API keys
# 3. Never commit .env to git (it's already gitignored)
#
# For Vercel deployment:
# Add these variables in Vercel Dashboard > Settings > Environment Variables

# ============================================
# REQUIRED (for OpenAI Provider)
# ============================================
# Get your OpenAI API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=
# ============================================
# OPTIONAL (Agent Configuration)
# ============================================
# Model to use (default: gpt-4o-mini)
# Options: gpt-4o-mini, gpt-4o, gpt-4-turbo, etc.
AGENT_MODEL=gpt-4o-mini

# Temperature for responses (0.0-1.0, default: 0.7)
AGENT_TEMPERATURE=0.7

# Maximum tokens in response (default: 4096)
AGENT_MAX_TOKENS=4096

# API timeout in seconds (default: 30)
AGENT_TIMEOUT=30

# ============================================
# NOTES
# ============================================
# - If OPENAI_API_KEY is not set, the system will use MockProvider (fallback)
# - MockProvider works without any API keys but has limited functionality
# - For production, always set OPENAI_API_KEY
